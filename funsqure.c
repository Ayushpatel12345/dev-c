#include<stdio.h>

int area(int side);

int main()
{
   int area1,side;
   printf("\n enter the side of square:");
   scanf("%d",&side);
   area1=area(side)
   return 0;   
}
int square(int side)
{
    return side*side;
}
